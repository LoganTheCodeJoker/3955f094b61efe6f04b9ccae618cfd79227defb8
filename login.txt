//login
            //get users from file
            var x = System.IO.File.ReadAllText(@"[PATH NAME HERE]");
            var y = x.Split('\n');
            string[] a = new string[2];
            List<users> u = new List<users>();
            
            //create list of users
            foreach (var z in y)
            {
                a = z.Split('|');
                u.Add(new users(a[0], a[1]));
            }

            var s = Console.ReadLine(); <- input id here
            var c = Console.ReadLine(); <- input password here
            
            //validation
            if (u.Contains(new users(s, c)))
                Console.Out.WriteLine(new users(s, c));  <- gib access
            else
                Console.Out.WriteLine("GTFO"); <- u have been hackz
            
            
            
 //user datatype
 public struct users
    {
        public string id;
        public string password;

        public users(string i, string p)
        {
            id = i;
            password = p;
        }

        public override string ToString()
        {
            return String.Format("{0} {1}", id, password);
        }
    }
   
/*user file: 
  id|password|
  id2|password2|
